import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.rigfindr',
  appName: 'rigfindr',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
